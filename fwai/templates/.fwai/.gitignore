# Auto-generated by fwai init
# Evidence runs and logs are local-only
runs/
logs/
*.log

# Private signing keys never leave this machine.
# keys/trusted/ is deliberately NOT ignored — those are public keys, and
# committing them is how the team agrees on whose evidence to believe.
keys/*.key
sessions/
